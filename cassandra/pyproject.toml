[project]
name = "cassandra"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "viki123v", email = "viktorhristovski629@gmail.com" }
]
requires-python = ">=3.13.10"
dependencies = []

[project.scripts]
cassandra = "cassandra:main"

[build-system]
requires = ["uv_build>=0.9.15,<0.10.0"]
build-backend = "uv_build"
